[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fixed-income-analytics"
version = "0.1.0"
description = "Fixed income quantitative analytics"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]